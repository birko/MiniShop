{% extends '::admin.html.twig' %}
{% block body %}
<h1>{% trans %}Coupon products add list{% endtrans %}</h1>
{{ products.render()| raw}}
<table class="records_list table table-striped">
    <thead>
        <tr>
            <td colspan="2">
                <a href="{{ path('coupon_products', {'id': entity.id}) }}" class="btn btn-xs btn-primary pull-right"><span class="glyphicon glyphicon-backward"></span> {% trans %}Back to coupon products list{% endtrans %}</a>
            </td>
        </tr>
        <tr>
            <th>{% trans %}Title{% endtrans %}</th>
            <th>{% trans %}Actions{% endtrans %}</th>
        </tr>
    </thead>
    <tfoot>
    </tfoot>
    <tbody>
    {% for product in products %}
        <tr>
            <td>{{ product.title }}</td>        
            <td>
                <a href="{{ path('coupon_products_add', { 'id': entity.id, 'product': product.id }) }}" class="btn btn-xsbtn-success"><span class="glyphicon glyphicon-plus"></span> {% trans %}Add{% endtrans %}</a>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>
{{ products.render()| raw}}
{% endblock %}
