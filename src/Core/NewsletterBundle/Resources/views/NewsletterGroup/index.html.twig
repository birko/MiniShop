{% extends '::admin.html.twig' %}
{% block body %}
<h1>{%trans%}Newsletter Group list{%endtrans%}</h1>

<table class="records_list table table-striped">
    <thead>
        <tr>
            <th>Name</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tfoot>
        <tr>
            <td colspan="2">
                <a href="{{ path('newsletter_group_new') }}" class="btn btn-primary">
                    <i class="icon-plus"></i> {%trans%}Create a new group{%endtrans%}
                </a>
           </td>
        </tr>
    </tfoot>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td>{{ entity.name }}</td>
            <td>
                 <a href="{{ path('newsletter_group_edit', { 'id': entity.id }) }}" class="btn btn-warning"><i class="icon-edit"></i> {%trans%}Edit{%endtrans%}</a>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>
{% endblock %}