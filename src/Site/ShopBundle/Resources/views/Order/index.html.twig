{% extends '::base.html.twig' %}
{% block body %}
<div class="box">
<h1>Objednávky</h1>
{{ knp_pagination_render(entities) }}
<table class="records_list table table-striped">
    <thead>
    </thead>
    <tfoot>
        <tr>
            <td colspan="2">
                <a href="{{ path('SiteUserBundle_index') }}" class="btn btn-inverse">{% trans %}Customer section{% endtrans %}</a>                
            </td>
        </tr>
    </tfoot>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td>{% if entity.createdAt %}{{ entity.createdAt|date('d.m.Y H:i:s') }}{% endif%}</td>
            <td>
                <a href="{{ path('order_detail', { 'id': entity.id }) }}" class="btn btn-inverse"><i class="icon-asterisk icon-white"></i> {% trans %}Show{% endtrans %}</a>
                <a href="{{ path('order_pdf', { 'id': entity.id }) }}" class="btn"><i class="icon-print"></i> {% trans %}Invoice{% endtrans %}</a>                
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>
{{ knp_pagination_render(entities) }}
{% endblock %}