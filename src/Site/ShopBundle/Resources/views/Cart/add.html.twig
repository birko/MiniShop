<form class="cart-add-form" action="{{ path('cart_add')}}" method="post">
    <div class="row">
        {% if options is not empty %}
        <table class="record_properties options pull-left col-sm-7 col-md-7 col-lg-7">
            <tbody>
                {% for option in options %}
                 <tr class="option">
                    <td class="control-group">
                        <select name="items[{{product}}][option][]" required="required" class="form-control">
                            <option value="">{{ option.0.name }}</option>                         
                            {% for value in option %}
                            <option value="{{value.id}}">{{ value.value }}</option>
                            {% endfor %}
                        </select>
                    </td>
                </tr>
                {% endfor %}
            </tbody>
        </table>
        {% endif %}
        <div class="col-sm-5 col-md-5 col-lg-5 pull-right">
          <input type="hidden" name="items[{{product}}][productId]" value="{{ product }}"/>
          <input type="hidden" name="items[{{product}}][price]" value="{{ price }}"/>
          <input type="hidden" name="items[{{product}}][priceVAT]" value="{{ priceVAT }}"/>
          <input type="hidden" name="items[{{product}}][name]" value="{{ title }}"/>
          <input type="hidden" name="items[{{product}}][amount]" value="{{ amount }}"/>
          <button type="submit" class="btn btn-default btn-block pull-right"><span class="glyphicon glyphicon-shopping-cart"></span> {% trans %}Add to cart{% endtrans %}</button>
        </div>
    </div>
</form>